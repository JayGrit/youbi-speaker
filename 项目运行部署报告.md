# ydbi-speaker 项目运行部署报告

## 1. 项目职责

`ydbi-speaker` 是 YouBi 流水线中的配音生成服务，对应阶段名为 `speaker`，数据库阶段表为 `yd_speaker`。当前实现已经从整任务 JSON 输入改为分段队列模式：服务轮询 `yd_speaker_segment` 中 `status = 'ready'` 的分段，下载 demucs 产出的人声音频，切出参考音频，调用 VoxCPM 生成目标语言 TTS 音频，并将每段结果上传到 MinIO。

在全链路阶段中的位置：

```text
downloader -> demucs -> whisper -> translator -> speaker -> combiner -> uploader
```

当某个任务下所有 speaker segment 都成功后，服务会 finalize 该任务，将 `yd_speaker.status` 标记为 `success`，并推进 `yd_combiner.status = ready`。

## 2. 当前运行环境

- Python 版本：`>=3.12`
- 包管理：标准 Python packaging，项目定义在 `pyproject.toml`
- 启动入口：`ydbi-speaker = ydbi_speaker.main:main`
- 推荐本地启动脚本：`./start.sh`
- 工作目录：默认 `/tmp/ydbi/speaker/<task_id>`，可通过 `YDBI_SPEAKER_WORK_DIR` 覆盖
- 轮询间隔：`10` 秒
- TTS 模型：`OpenBMB/VoxCPM2`
- 当前运行方式：支持本地运行，也提供 Dockerfile

容器内外路径差异：

| 项目 | 本地运行 | 容器运行 |
| --- | --- | --- |
| `YDBI_ROOT` | 默认当前 speaker 项目目录 | 不需要 |
| `WORKFOLDER` | `./workfolder` | `/work` |
| `MODEL_CACHE_DIR` | `./data/modelscope` | `/models/modelscope` |
| `VOXCPM_MODEL_DIR` | `$MODEL_CACHE_DIR/OpenBMB__VoxCPM2` | `/models/VoxCPM2` |

## 3. 配置说明

主要配置集中在 `ydbi_speaker/config.py`。

### 数据库

`MYSQL_CONFIG` 当前包含：

- `host`: `120.53.92.66`
- `port`: `3306`
- `user`: `hoshuuch`
- `database`: `youbi`
- `password`: 已硬编码在 `config.py`，报告不展开明文

### 对象存储

当前使用 MinIO：

- `STORAGE_BACKEND`: `minio`
- `MINIO_ENDPOINT`: `http://120.53.92.66:9000`
- `MINIO_BUCKET`: `ydbi`
- `MINIO_FULL_BASE_URL`: `https://120.53.92.66/minio`
- `MINIO_SECURE`: `False`
- `MINIO_ACCESS_KEY` / `MINIO_SECRET_KEY`: 已硬编码在 `config.py`，报告不展开明文

### VoxCPM 配置

- `VOXCPM_MODEL`: `OpenBMB/VoxCPM2`
- `VOXCPM_MODEL_DIR`: 本地或容器内模型目录；本地默认位于当前项目目录 `./data/modelscope/OpenBMB__VoxCPM2`
- `VOXCPM_LOAD_DENOISER`: `False`
- `VOXCPM_MIN_REFERENCE_MS`: `1200`
- `VOXCPM_CFG_VALUE`: `2.0`
- `VOXCPM_INFERENCE_TIMESTEPS`: `10`

如果本地 `VOXCPM_MODEL_DIR` 不存在，服务会自动通过 ModelScope 下载 `OpenBMB/VoxCPM2` 到当前项目目录下的 `data/modelscope/OpenBMB__VoxCPM2`。使用方通常只需要执行 `./start.sh`。

### 运行身份

服务通过环境变量 `DEVICE` 标记当前运行机器或 operator：

- 未设置时默认为 `Macbook Air M4`
- 可选心跳列包括：`Macbook Air M4`、`Macmini M2`、`LPXB`、`MY_HP`、`LPXB_HP`、`TXY`
- `DEVICE` 会写入 `yd_task`、`yd_speaker`、`yd_speaker_segment` 的 `operator` 字段

## 4. 环境依赖

`pyproject.toml` 声明的 Python 依赖：

```text
audiostretchy
librosa
minio>=7.2.0
modelscope
mysql-connector-python>=9.0.0
numpy
pydub
soundfile
voxcpm
```

Docker 约束文件 `docker-constraints.txt` 固定了容器依赖版本：

```text
torch==2.11.0
torchaudio==2.11.0
voxcpm==2.0.2
audiostretchy==1.3.5
librosa==0.11.0
modelscope==1.36.3
mysql-connector-python==9.7.0
numpy==2.4.4
pydub==0.25.1
soundfile==0.13.1
```

系统依赖：

- `ffmpeg`
- `libsndfile1`

Dockerfile 已安装这两个系统依赖。

## 5. 数据流与产物

### 输入

服务处理的是 `yd_speaker_segment` 分段任务，而不是整任务 JSON。

分段任务需要包含：

- `task_id`
- `item_index`
- `dst_text`
- `start_time`
- `end_time`
- `status = 'ready'`

同时对应任务需要满足：

- `yd_speaker.status IN ('ready', 'running')`
- `yd_video_info.audio_vocals_path` 或 `audio_vocals_url` 可用
- 上游 translator 已准备好 segment 数据

人声音频下载逻辑：

1. 如果 demucs 的 `operator` 与当前服务相同，并且本地 `audio_vocals_path` 存在，则优先使用本地文件
2. 否则使用 `audio_vocals_url`
3. 如果 URL 缺失但本地路径存在，则尝试本地路径和 MinIO 对象候选
4. 额外会尝试候选对象：

```text
<task_id>/media/audio_vocals.wav
<task_id>/demucs/audio_vocals.wav
<task_id>/audio_vocals.wav
```

### 参考音频选择

服务会先根据 `yd_asr_segment` 中 `segment_type = 'fixed'` 的结果切分参考音频；如果没有 fixed，则使用 `raw`；如果都没有，则退回到 `yd_speaker_segment`。

参考音频会按照以下指标评分：

- 时长
- 响度
- 语音密度
- 削波情况
- 源文本长度
- 响度稳定性

最佳参考音频会保存为：

```text
<work_dir>/<task_id>/speaker/reference.wav
<work_dir>/<task_id>/speaker/reference_candidates.json
```

每个分段的人声参考会保存为：

```text
<work_dir>/<task_id>/segments/vocals/0001.wav
<work_dir>/<task_id>/segments/vocals/0002.wav
...
```

### TTS 输出

每个分段的 TTS 输出：

```text
<work_dir>/<task_id>/segments/tts/0001.wav
<work_dir>/<task_id>/segments/tts/0002.wav
...
```

上传到 MinIO：

```text
<task_id>/speaker/vocals/<segment>.wav
<task_id>/speaker/tts/<segment>.wav
```

写回 `yd_speaker_segment`：

- `reference_wav_path`
- `reference_wav_url`
- `tts_wav_path`
- `tts_wav_url`

任务 finalize 后写入 `yd_video_info`：

- `translation_json_path = db://yd_speaker_segment/<task_id>`
- `vocals_segments_dir = s3://ydbi/<task_id>/speaker/vocals/`
- `tts_segments_dir = s3://ydbi/<task_id>/speaker/tts/`

## 6. 数据库表与状态推进

服务使用的核心表：

- `yd_task`
- `yd_speaker`
- `yd_speaker_segment`
- `yd_translator`
- `yd_combiner`
- `yd_demucs`
- `yd_video_info`
- `yd_asr_segment`
- `yd_service_heartbeat`

`yd_speaker_segment` 会自动创建或补充字段，关键字段包括：

- `task_id`
- `item_index`
- `status`
- `src_text`
- `dst_text`
- `start_time`
- `end_time`
- `speaker`
- `reference_wav_path`
- `reference_wav_url`
- `tts_wav_path`
- `tts_wav_url`
- `attempt_count`
- `max_attempts`
- `operator`
- `error_message`

分段状态流转：

1. `ready`
2. `running`
3. 成功后 `success`
4. 失败后如果未达到 `max_attempts`，回到 `ready`
5. 达到最大尝试次数后标记 `failed`，并将整任务标记失败

任务状态流转：

1. `yd_speaker.status = ready`
2. 首个分段被 claim 后更新为 `running`
3. 所有分段 `success` 后 finalize 为 `success`
4. 推进 `yd_combiner.status = ready`

## 7. 本地部署手册

### 1. 准备 Python 环境

```bash
cd /path/to/YouBi/services/speaker
python3.12 -m venv .venv
source .venv/bin/activate
python -m pip install -U pip
python -m pip install -e .
```

### 2. 安装系统依赖

确保 `ffmpeg` 可用，并确保 `libsndfile` 可被 `soundfile` 访问。

macOS 可使用：

```bash
brew install ffmpeg libsndfile
```

### 3. 准备 VoxCPM 模型

本地运行不要求预先指定模型环境变量。默认模型目录位于当前 speaker 项目内：

```text
./data/modelscope/OpenBMB__VoxCPM2
```

如果该目录不存在，程序首次加载 VoxCPM 时会自动创建 `./data/modelscope` 并下载 `OpenBMB/VoxCPM2`。

容器运行时要求模型挂载到：

```text
/models/VoxCPM2
```

### 4. 配置运行机器标识

```bash
export DEVICE="Macbook Air M4"
```

如需改变工作目录：

```bash
export YDBI_SPEAKER_WORK_DIR="/tmp/ydbi/speaker"
```

### 5. 启动服务

推荐使用启动脚本：

```bash
./start.sh
```

脚本会自动设置：

```text
YDBI_ROOT
WORKFOLDER
MODEL_CACHE_DIR
VOXCPM_MODEL_DIR
YDBI_SPEAKER_WORK_DIR
```

如果不用脚本，也可以直接运行：

```bash
ydbi-speaker
```

或：

```bash
python -m ydbi_speaker.main
```

服务启动后会持续轮询 `yd_speaker_segment`。

## 8. Docker 部署手册

### 1. 构建镜像

```bash
cd /path/to/YouBi/services/speaker
docker build -t ydbi-speaker:latest .
```

`Dockerfile` 使用：

- `python:3.12-slim`
- `ffmpeg`
- `libsndfile1`
- CPU 版 `torch==2.11.0`
- `docker-constraints.txt` 固定 Python 依赖

### 2. 运行容器

示例：

```bash
docker run --rm \
  --name ydbi-speaker \
  -e DEVICE="Macmini M2" \
  -v "$YDBI_ROOT/workfolder:/work" \
  -v "$YDBI_ROOT/data/modelscope/OpenBMB__VoxCPM2:/models/VoxCPM2:ro" \
  ydbi-speaker:latest
```

容器需要能访问远端 MySQL 与 MinIO。

## 9. 运维检查

### 心跳检查

查看 `yd_service_heartbeat` 中 `service_name = 'speaker'` 的对应设备列是否持续更新。

### 分段任务检查

关注：

- `yd_speaker.status`
- `yd_speaker.error_message`
- `yd_speaker_segment.status`
- `yd_speaker_segment.attempt_count`
- `yd_speaker_segment.error_message`
- `yd_speaker_segment.tts_wav_url`

### Finalize 检查

成功任务应满足：

```sql
SELECT COUNT(*) FROM yd_speaker_segment WHERE task_id = '<task_id>' AND status <> 'success';
SELECT translation_json_path, vocals_segments_dir, tts_segments_dir
FROM yd_video_info
WHERE task_id = '<task_id>';
```

MinIO 应存在：

```text
s3://ydbi/<task_id>/speaker/vocals/
s3://ydbi/<task_id>/speaker/tts/
```

## 10. 常见问题

- VoxCPM 模型目录不存在：本地运行执行 `./start.sh`，程序会自动下载到项目内 `data/modelscope/OpenBMB__VoxCPM2`；容器运行检查 `/models/VoxCPM2` 挂载
- 没有 ready 分段：检查 translator 是否已写入 `yd_speaker_segment`
- 任务无法 finalize：检查是否仍有分段不是 `success`
- 人声音频下载失败：检查 `yd_video_info.audio_vocals_url` 和 MinIO 对象
- 参考音频不可用：检查 `yd_asr_segment` 或 `yd_speaker_segment` 的时间轴是否合理
- 单段反复失败：检查 `dst_text` 是否为空、参考音频是否过短、VoxCPM 是否加载成功

## 11. 当前风险与改进建议

- 数据库密码和 MinIO 密钥硬编码在源码中，建议改为环境变量或密钥管理
- VoxCPM 本地模型会自动下载到项目内 `data/modelscope/OpenBMB__VoxCPM2`；首次启动需要能访问 ModelScope
- 当前 Dockerfile 使用 CPU 版 torch，长音频或大量分段时性能可能不足
- `Dockerfile` 与 `Dockerfile.runtime` 内容相同，可考虑合并或明确区分构建镜像与运行镜像
- `handle()` 已明确废弃整任务 JSON 输入，调用方必须确保写入 `yd_speaker_segment`
